def readProp;
script {
    node () {
        checkout scm
        readProp = readProperties(file: 'test.yml')
    }
}


pipeline {
    agent any

    stages {
        stage('ymalfile') {
            steps {
                script {
                    echo """${readProp['test.firstname']}"""
                }
            }
        }
    }
}