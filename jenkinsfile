def readProp;
testtemplate {
    readProp = readProperties(file: 'test.yml')
}


pipeline {
    agent any

    stages {
        stage('ymalfile') {
            steps {
                script {
                    echo """${readProp['firstname']}"""
                }
            }
        }
    }
}