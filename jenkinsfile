pipeline {
    agent any

    stages {
        stage('ymalfile') {
            steps {
                script {
                    def readProp;
                    readProp = readProperties(file: 'test.yml')
                    echo """${readProp['firstname']}"""
                }
            }
        }
    }
}